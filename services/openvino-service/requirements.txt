# OpenVINO Service Dependencies (Production)
# Phase 1: Containerized AI Models

# Web Framework
fastapi>=0.123.0,<0.124.0  # Stable 0.123.x series (December 2025)
uvicorn[standard]>=0.32.0,<0.33.0  # Stable 0.32.x series with standard extras

# Data Validation
pydantic==2.12.4

# Data Processing
numpy>=1.26.0,<1.27.0  # Stable NumPy 1.26.x (CPU-only, compatible with OpenVINO and transformers)

# Simplified Model Stack (Standard models for compatibility)
# CPU-only PyTorch index (no CUDA - saves 8.5GB!)
--extra-index-url https://download.pytorch.org/whl/cpu

sentence-transformers==3.3.1  # Embeddings: BAAI/bge-large-en-v1.5 (1024-dim) [Epic 47]
transformers==4.46.1          # HuggingFace models (compatible with sentence-transformers)
torch>=2.4.0,<3.0.0  # Stable PyTorch 2.4.x CPU-only (November 2025)
sentencepiece                 # Required for T5 tokenizer
# Removed OpenVINO for now due to dependency conflicts
