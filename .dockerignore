# Root .dockerignore - Excludes large directories when building with context: .
# This dramatically reduces build context size and speeds up builds

# Node.js
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm
.yarn-integrity
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Python
# NOTE the **/ prefixes: Docker matches .dockerignore patterns against the whole
# path and a bare * does not cross a /, so `__pycache__/` alone excludes only a
# top-level directory. Nested ones (domains/**/src/**/__pycache__) shipped into
# every image -- 260 of them -- which is how a .pyc for a deleted module stayed
# in the built image after its source was removed.
**/__pycache__/
**/*.py[cod]
**/*.pyo
**/*.pyd
**/.pytest_cache/
.Python
*.so
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Models and data
# Exclude models/data directories globally, but allow service-specific ones
# IMPORTANT: Order matters - exceptions must come AFTER general exclusions
models/
!services/**/models/  # Allow service-specific models directories
data/
!services/**/data/  # Allow service-specific data directories (e.g., ha-simulator)
!domains/data-collectors/zeek-network-service/data/ieee-oui.tsv.gz  # Vendored IEEE OUI snapshot (TAP-6577); IEEE blocks CI egress IPs, so the Dockerfile COPYs this instead of fetching it at build time
# Database files (exclude everywhere except service data directories)
*.db
!services/**/data/**/*.db
*.sqlite
*.sqlite3

# Logs
logs/
*.log
*.log.*

# Test artifacts
test-results/
test_output.txt
test_result.json
coverage_html/
coverage.xml
.pytest_cache/

# Documentation (large and not needed in images)
docs/
!docs/mcp/homeiq-mcp-tools.schema.json
implementation/
*.md
!README.md
CHANGELOG.md
CONTRIBUTING.md
DEPLOY_YAML_VALIDATION.md
CLAUDE.md

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace

# Git
.git/
.gitignore
.gitattributes
.github/

# Docker
docker-compose*.yml
Dockerfile*
.dockerignore
*.dockerignore

# CI/CD
.github/
.gitlab-ci.yml
.travis.yml
.circleci/
Jenkinsfile

# Build artifacts
*.tar.gz
*.zip
*.rar
*.7z

# Simulation and test results
simulation/
simulation_results/
simulation_results_comprehensive/
simulation_results_fresh/
simulation_results_gnn/
test-results/

# Infrastructure config (may contain secrets, exclude from images)
infrastructure/env.*
infrastructure/.env*
!infrastructure/env.example
!infrastructure/env.template*

# Temporary files
tmp/
temp/
*.tmp
*.temp
nul

# Debug and analysis files
debug_prompt*.json
conversation-analysis.json
enhanced-prompt.md

# Reports
reports/

# TappsCodingAgents (framework development files, not needed in service images)
TappsCodingAgents/
.tapps-agents/

# CDK infrastructure code
cdk/

# Issues and planning
issues/

# Scripts (usually not needed in runtime images)
scripts/
tools/

# Simulation code
simulation/

# Workspace config
*.code-workspace
homeiq.code-workspace

# Package files (not needed in images)
package.json
package-lock.json
pyproject.toml
requirements*.txt
!services/**/requirements*.txt
!services/**/package*.json

# Root-level test files
conftest.py
pytest-unit.ini
run-unit-tests.*
run_embedding_test.sh

# Various utility scripts at root
populate_*.py
store_*.py
get_validation_errors.py
direct_store_docs.py
verify_*.py
test_*.py

