# Automation Miner - Community Knowledge Crawler
# Story AI4.1: Community Corpus Foundation

# HTTP Client (Context7 validated: async, retry, timeout)
httpx>=0.27.0,<0.29.0  # Phase 1: aligned

# HTML Parsing
beautifulsoup4>=4.14.2
lxml>=6.0.2

# Data Validation (Context7 validated: BaseModel, field validators)
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0  # Phase 1: aligned

# API Framework
fastapi>=0.115.0,<1.0.0  # Phase 1: aligned
uvicorn[standard]>=0.30.0,<1.0.0  # Phase 1: aligned

# Database (Context7 validated: async ORM)
sqlalchemy[asyncio]>=2.0.36,<2.1.0  # Phase 1: aligned
aiosqlite>=0.20.0,<0.23.0  # Phase 1: aligned
alembic>=1.13.0,<2.0.0  # Phase 1: aligned
asyncpg>=0.30.0  # Phase 1C: PostgreSQL migration

# Job Scheduling (Context7 validated: AsyncScheduler, CronTrigger)
apscheduler>=3.10.0

# CLI
click>=8.1.0

# YAML Parsing
pyyaml>=6.0.1,<7.0.0  # Phase 2: standardized

# Fuzzy String Matching (deduplication)
rapidfuzz>=3.14.3

# Logging
python-json-logger>=2.0.0

