"""Dashboard views (Streamlit UI modules).

Kept out of a top-level ``pages/`` directory so Streamlit does not treat this
package as multipage routes (which would duplicate ``main.py`` navigation).
"""
