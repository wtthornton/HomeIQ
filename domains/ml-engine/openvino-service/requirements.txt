# OpenVINO Service Dependencies (Production)
# Phase 1: Containerized AI Models

# Web Framework
fastapi>=0.115.0,<1.0.0  # Phase 1: aligned
uvicorn[standard]>=0.30.0,<1.0.0  # Phase 1: aligned

# Data Validation
pydantic>=2.9.0,<3.0.0  # Phase 1: aligned

# Data Processing
numpy>=1.26.0,<3.0.0  # Phase 3: allow 1.x and 2.x (Feb 2026 upgrade)

# Simplified Model Stack (Standard models for compatibility)
# CPU-only PyTorch index (no CUDA - saves 8.5GB!)
--extra-index-url https://download.pytorch.org/whl/cpu

sentence-transformers>=5.0.0,<6.0.0  # Embeddings: BAAI/bge-large-en-v1.5 (1024-dim) [Epic 38]
transformers==4.46.1          # HuggingFace models (compatible with sentence-transformers)
torch>=2.5.0,<3.0.0  # Feb 2026 upgrade — aligned with activity-recognition, CPU-only
sentencepiece                 # Required for T5 tokenizer
# Removed OpenVINO for now due to dependency conflicts
