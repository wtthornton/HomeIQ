# Device Intelligence Service Dependencies

# FastAPI and ASGI server
fastapi>=0.115.0,<1.0.0  # Phase 1: aligned
uvicorn[standard]>=0.30.0,<1.0.0  # Phase 1: aligned

# Pydantic for settings and data validation
pydantic>=2.9.0,<3.0.0  # Phase 1: aligned
pydantic-settings>=2.6.0,<3.0.0  # Phase 1: aligned

# Database and ORM
sqlalchemy>=2.0.36,<2.1.0  # Phase 1: aligned
alembic>=1.13.0,<2.0.0  # Phase 1: aligned
asyncpg>=0.30.0  # Phase 1C: PostgreSQL migration

# HTTP client for external APIs
httpx>=0.27.0,<0.29.0  # Phase 1: aligned
aiohttp>=3.13.3,<4.0.0  # CVE-2025-69223..69230 fix — min 3.13.3
tenacity>=8.2.0,<10.0.0  # Phase 2: standardized

# YAML configuration support (Epic AI-24: Device Mapping Library)
PyYAML>=6.0.1,<7.0.0  # YAML configuration files for device mappings

# WebSocket client
websockets>=12.0,<13.0.0  # Unpinned for patch updates

# MQTT client
paho-mqtt>=1.6.1,<2.0.0  # Unpinned for patch updates

# System monitoring
psutil>=7.1.3,<8.0.0  # Unpinned for patch updates

# Development and testing
pytest>=8.3.0,<10.0.0  # Phase 2: standardized
pytest-asyncio>=0.23.0,<2.0.0  # Phase 2: standardized
pytest-cov>=5.0.0,<6.0.0  # Phase 2: standardized

# Code quality
black>=23.11.0,<24.0.0  # Unpinned for patch updates
isort>=5.12.0,<6.0.0  # Unpinned for patch updates
flake8>=6.1.0,<7.0.0  # Unpinned for patch updates

# Type checking
mypy>=1.7.1,<2.0.0  # Unpinned for patch updates
types-psutil>=6.1.0.20241022  # Unpinned for patch updates (types packages use date versions)

# Machine Learning and Analytics - 2026 Release (Phase 3: ML/AI Library Upgrades, Epic 71)
scikit-learn>=1.8.0,<2.0.0  # Phase 3: upgraded from 1.5.x → 1.8.0 (Mar 2026). IMPORTANT: .pkl models must be regenerated
pandas>=3.0.1,<3.1.0  # Phase 3: upgraded from 2.2.x → 3.0.1 (Feb 2026). PyArrow string dtype default
numpy>=2.4.3,<3.0.0  # Phase 3: upgraded from 1.26.x → 2.4.3 (Mar 2026). NEP 50 dtype promotion
joblib>=1.5.3,<2.0.0  # Phase 3: upgraded from 1.3.x → 1.5.3 (Mar 2026)
pyarrow>=18.0.0  # Phase 3: required by Pandas 3.0 for default string dtype
lightgbm>=4.0.0,<5.0.0  # LightGBM for faster training (2-5x speedup, 2025 improvement)
tabpfn>=2.2.0,<7.0.0  # TabPFN for instant predictions (5-10x faster, 2025 improvement)
river>=0.21.0,<1.0.0  # River for incremental learning (10-50x faster updates, 2025 improvement)

# Scheduling
apscheduler>=3.10.0,<4.0.0  # Stable task scheduler (Epic 46.2: Built-in Nightly Training Scheduler)