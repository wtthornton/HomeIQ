# model-server Dependencies (Production)
# Merged from openvino-service, ml-service, rag-service (TAP-7276).
# One process: OpenVINO inference, classical ML algorithms, and RAG retrieval.

# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Data Validation
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# Data Processing (numpy pinned to the tighter of the two former ranges)
numpy>=2.4.3,<3.0.0
scipy>=1.17.1,<2.0.0

# Classical ML (former ml-service)
scikit-learn>=1.8.0,<2.0.0

# OpenVINO / embeddings model stack (former openvino-service)
# CPU-only PyTorch index (no CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=5.0.0,<6.0.0  # Embeddings: BAAI/bge-large-en-v1.5 (1024-dim)
transformers>=5.5.0,<6.0.0   # CVE-2026-4372/CVE-2026-5241 fix (TAP-6622)
torch>=2.5.0,<3.0.0
sentencepiece                 # Required for T5 tokenizer

# Database (former rag-service)
sqlalchemy>=2.0.36,<2.1.0
asyncpg>=0.30.0
alembic>=1.13.0,<2.0.0

# HTTP clients (used by opentelemetry-instrumentation-httpx and the test suite's
# ASGI transport; the in-process OpenVINO bridge no longer needs it at runtime)
httpx>=0.27.0,<0.29.0

# Utilities
python-dotenv>=1.0.0,<2.0.0

# OpenTelemetry Observability (optional, former rag-service)
opentelemetry-api>=1.24.0,<2.0.0
opentelemetry-sdk>=1.24.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.45b0,<1.0.0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0,<2.0.0
opentelemetry-instrumentation-httpx>=0.45b0,<1.0.0

# HomeIQ shared libraries (installed from libs/ in Docker)
homeiq-observability>=1.0.0
homeiq-data>=1.0.0
