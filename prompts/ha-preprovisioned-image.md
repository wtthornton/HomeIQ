# TAP-6483 — Build the pre-provisioned Home Assistant image

> Generated by the `orchestration-prompt` skill. Run from a session whose cwd is
> `/home/wtthornton/code/HomeIQ`.

## Prerequisites / Wayfind gate

- **Route clear?** Yes. `docs/architecture/adr-appliance-packaging.md` locks the four
  packaging decisions (headless pre-provisioned HA, HACS build-time only, no
  Supervisor, installer + pinned compose bundle). TAP-6483 `blockedBy` is empty; it
  blocks TAP-6484/6485/6486. No open `wayfinder:map`.
- **Chunk mix:** all execute / verify / fix. Nothing here is a Decide chunk.
- **Wayfind resume:** `uv run tapps-mcp memory search --query "wayfind appliance packaging HA image"`
  — fold any `memory_group=wayfind` hits into Context. Skip silently if brain is offline.

## How to run (cold start — paste into a NEW session)

- **Goal loop (recommended):**
  `Read prompts/ha-preprovisioned-image.md in full, run the Prerequisites gate, then execute it as a goal loop — run the Loop section repeatedly until Done-when holds, printing the SCORE line every iteration. Establish your own preconditions per Sub-goal 0; do not stop unless an Autonomy hard-stop fires.`
- **Resuming mid-run (after a checkpoint):** `/tapps-continue-session` first, then the
  line above. Re-verify live state before acting on any handoff claim.

## Standing constraints

Each also appears as an Autonomy hard-stop below.

- **No workarounds, no green-by-suppression.** Never `try/except: pass`, never skip or
  disable a failing test, never `# noqa` / `# type: ignore` / `eslint-disable`, never
  hardcode a value to force a pass, never leave a stub and call it done. If the correct
  fix is out of scope or you are blocked, **stop and surface it** — do not ship the
  workaround.
- **Do not enable HACS at runtime** to make a component appear. Vendoring at build time
  is the decision; reaching for HACS is the workaround this story exists to remove.
- **Writes stay in this repo.** No cross-repo or cross-project writes.
- **Linear writes route through the `linear-issue` skill**, never raw `save_issue`.
  Assignee is the agent (`Claude Agent`, `9083b7a1-3fd3-479b-98f1-1f8a782ae10a`), never
  the OAuth human.
- **Do not merge to master.** A draft PR is the terminal artifact.

## Objective

Ship a reproducible, pinned Home Assistant image carrying the four vendored components
with no HACS present, plus a manifest CI enforces.

## Done-when (ground truth, not narration)

All of the following pasted as command output in the transcript:

1. `docker run` of the built image reaches a serving state — `curl` of
   `/manifest.json` returns HTTP 200, output pasted. **Not `/api/`:** HA answers
   `/api/` with 401 and `/` with 302 without a bearer token, and `curl -f` fails
   on 401, so an `/api/` check can never pass on a correctly running instance.
   (Corrected 2026-08-23 after the first run; the original clause was
   unreachable — a cap that fires on correct behavior.)
2. `docker run --rm --entrypoint ls <image> /config/custom_components/` lists **exactly the
   4 enumerated components** — `homeiq`, `powercalc`, `teamtracker`, and the Aqara FP1E
   quirk path — and **4 of 4 is the enumerated total**; a run showing fewer is a FAIL, not
   a pass. (Count must not shrink: the goal is unreachable by deleting a component.)
3. The same listing contains **no** `hacs` directory — a *structural* check.
   **Not a substring grep:** `grep -ri hacs` matches the Hungarian word "hacsak"
   in a Powercalc translation and would fail a correct image. (Corrected
   2026-08-23 after the first run.)
4. The pinned HA version in the Dockerfile is a concrete release `>= 2026.8.0`, never
   `stable` — `grep -n 'FROM ghcr.io/home-assistant' <dockerfile>` pasted.
5. `python scripts/check-ha-component-manifest.py` exits 0 against the built image, and
   exits **non-zero** when a component version is perturbed — both runs pasted (proving
   the check can actually fail).
6. Every `VAL-` ID below is verified by an independent verifier with evidence quoted.

## Validation contract (filled before execution)

| ID | Behavioral assertion | Fulfilled by | Evidence tool |
|----|----------------------|--------------|---------------|
| VAL-001 | Image builds from a pinned HA release `>= 2026.8.0`, not `stable` | SG2 | `grep FROM` + `docker build` exit 0 |
| VAL-002 | `/config/custom_components/` contains `homeiq` | SG3 | `docker run --entrypoint ls` |
| VAL-003 | `/config/custom_components/` contains `powercalc` | SG3 | same |
| VAL-004 | `/config/custom_components/` contains `teamtracker` | SG3 | same |
| VAL-005 | The FP1E quirk is at `/config/custom_zha_quirks/` and the seed points `zha.custom_quirks_path` there | SG3 | `ls` + `cat configuration.yaml` |
| VAL-006 | No structural HACS artifact in the shipped image | SG3 | `test -e /config/custom_components/hacs` (structural, not substring grep) |
| VAL-007 | Manifest records the HA pin + every vendored component version, and CI fails on disagreement | SG4 | `check-ha-component-manifest.py` pass **and** perturbed-fail |
| VAL-008 | Container starts and serves with the seeded config | SG5 | `curl /manifest.json` HTTP 200 + docker healthcheck `healthy` |

Coverage: 8 IDs, each claimed exactly once. Done-when requires all 8 green.

## Sub-goals (sequential; each a checkpoint)

0. **Establish preconditions (self-healing — the loop sets these up, not the user).**
   - Branch: `git checkout -b tap-6483-build-the-pre-provisioned-home-assistant-image-with-vendored`
     off current `origin/master`. Confirm with `git merge-base --is-ancestor HEAD origin/master`.
   - `docker info` succeeds; enough disk for an HA image (~2 GB).
   - **Resolve the pin:** find the latest HA release `>= 2026.8.0` (the floor from
     `custom_components/homeiq/manifest.json:13`) and record how you resolved it.
   - **Artifact identity — the live trap here.** `domains/core-platform/compose.yml:688`
     declares `image: ghcr.io/home-assistant/home-assistant:stable` with **no `build:`
     key**, so adding a Dockerfile alone changes nothing that compose runs. Both failures
     are required-fail caps:
     - *Divergent (built ≠ loaded):* after wiring `build:`, compare the running
       container's image id to the id just built (`docker inspect --format '{{.Image}}'`
       vs `docker images -q`). Equal ids, or a sentinel string from the new source found
       inside the running container. **Never trust the build's exit code.**
     - *Stale (merged ≠ live):* if a prior image exists, confirm it is not older than the
       branch tip before testing against it.
   - **Smoke before spend:** `curl -f http://localhost:8124/api/` against the running
     fixture before doing real work.
   - proof: branch created, `docker info` ok, pin recorded, identity check pasted.
1. **Correct the stale premises and finalize the contract** (cheapest first) —
   see Unverified assumptions. `hacs.json` is already deleted by `32166a1c`; **verify and
   record, do not redo**. Confirm the seed gap and the `zeek:latest` correction.
   proof: contract table above complete, coverage check pasted, AC7 recorded as met by `32166a1c`.
2. **Write the Dockerfile with the pinned base.** — fulfills VAL-001 —
   proof: `docker build` exit 0 and `grep -n 'FROM ghcr.io/home-assistant'` pasted.
3. **Vendor the four components into `/config/custom_components/`.** Source them at build
   time; HACS must not survive into the image. — fulfills VAL-002..VAL-006 —
   proof: `docker run --rm --entrypoint ls <image> /config/custom_components/` + `grep -ri hacs`.
4. **Write the version manifest + `scripts/check-ha-component-manifest.py`**, wired into
   `.github/workflows/quality-gate.yml` beside the existing
   `python scripts/check-container-budget.py` step (line 64) — that is the precedent to
   match. — fulfills VAL-007 — proof: clean run exit 0 **and** perturbed run non-zero.
5. **Seed `configuration.yaml` and prove the image serves.** The fixture mounts the named
   volume `ha_test_config`, so no seed exists in-repo today — create one. — fulfills
   VAL-008 — proof: `curl -f http://localhost:8123/api/` HTTP 200 pasted.
6. **Open a draft PR** referencing TAP-6483. Do not merge.

## Plane map (mechanism + literal dispatch parameters)

`effort` applies only inside a Workflow — the Agent tool has no effort parameter.

| Step | Plane | Mechanism | agentType | model | effort | Notes |
|------|-------|-----------|-----------|-------|--------|-------|
| Premise re-check (SG1) | coordination | subagent | `Explore` | `haiku` | n/a | closed, evidence-checkable questions only |
| Resolve HA pin (SG0) | coordination | `tapps_research` / web | n/a | n/a | n/a | research-to-execute; route is already clear |
| Dockerfile + vendoring (SG2–3) | execution | direct edit, this repo | `general-purpose` | `sonnet` | n/a | **serial** — one file at a time, coupled |
| Manifest + CI check (SG4) | execution | direct edit | `general-purpose` | `sonnet` | n/a | match the container-budget precedent |
| Seed + serve (SG5) | execution | direct edit + `docker run` | `general-purpose` | `sonnet` | n/a | |
| **Verify each sub-goal** | coordination | **verifier subagent, fresh context** | `general-purpose` | **`opus`** | n/a | creator ≠ verifier; refutes the proof |

**N=1, single coupled item → this is a `/goal` drive, not a Workflow.** Do not emit or
invoke a Workflow script for this prompt.

## Loop

- **State:** read first — `/tapps-continue-session` if resuming, `git status`,
  `get_issue(id="TAP-6483")`, brain recall of prior attempts on this story.
- **Decide:** pick the lowest-numbered sub-goal not yet verified green.
- **Execute:** the action, on the committed mechanism above.
- **Verify (independent):** dispatch
  `Agent(subagent_type: "general-purpose", model: "opus", prompt: "<refute this proof>")`
  with a *fresh* context. **Hand it the exact proof command, the expected artifact, the
  file:line anchors, and the environment quirks** (HA on 8123 inside the container, 8124
  on the host; `docker run --entrypoint ls` needed because the HA image's entrypoint is
  not a shell) — never your narrative, or it will reason about plausibility instead of
  running anything. Its report must quote the output it observed. Its verdict, not your
  claim, advances the loop.
- **On fail (expected-fail fix loop):** record a structured handoff → scope a narrow fix
  sub-goal → re-execute → re-verify. ≤**3** validation rounds per sub-goal, then escalate
  once, then stop with a diagnosis. Never weaken the contract to go green.
- **Record:** completed · undone · commands + exit codes · issues · failure-and-why → brain.
- **Context hygiene:** prune stale reads; targeted `grep` over a full re-Read; carry a
  compact state summary, not raw transcripts.
- **Checkpoint (shift boundary):** at each sub-goal boundary or ~50% context — whichever
  first — run `/tapps-handoff-session`, then clear for real and resume via
  `/tapps-continue-session`. See Checkpoint protocol. Never instruct yourself to run
  `/clear`; an agent cannot invoke a built-in CLI command.
- **Print every iteration:**
  `SCORE: <n>/8 VAL green · sub-goal <k>/6 · round <a>/3 · iteration <i>/12`
- **Repeat or stop:** until Done-when holds. Caps: **12 iterations** AND **400k tokens**,
  both **cumulative across shifts**, read from the handoff, never reset by a checkpoint.

## Checkpoint protocol (context shift boundary)

- **Lane:** declared checkpoint (attended). Print the block below, then stop.
- **Trigger:** sub-goal boundary, or ~50% context — whichever first.
- **Write:** `/tapps-handoff-session`. **Resume:** `/tapps-continue-session`.
- **Carry-forward (must survive the clear, or the guardrails stop binding):** current
  sub-goal + its VAL IDs · attempt count vs cap, cumulative (`round 2 of 3`) · budget
  spent, cumulative · strategies already tried and refuted and why · the resume line above.
- **On resume, re-verify before acting:** the handoff is a pointer, not a proof. Re-check
  `git rev-parse --short HEAD`, PR state, `git merge-base --is-ancestor HEAD origin/master`,
  and the built image id. A claim true at checkpoint can be false an hour later.

```
CHECKPOINT <n> — sub-goal <k> complete. Handoff written.
Cumulative: round <a> of 3 · budget <spent>/400k.
Next: /clear   then   /tapps-continue-session
```

## Guardrails

- Termination: Done-when above; caps 12 iterations AND 400k tokens, cumulative.
- Standing constraints: every bullet in that section, each also an Autonomy hard-stop.
- No green-by-deletion: Done-when clause 2 requires **4 of 4** enumerated components, so
  the goal cannot be met by dropping one.
- Independent verification (creator ≠ verifier); the verifier gets the **proof command**,
  not the claim; expected-fail fix loop, ≤3 rounds per sub-goal.
- Caps must not fire on correct behavior: the perturbed-manifest run in VAL-007 is
  *supposed* to exit non-zero — that is a pass, not a failure.
- No fan-out of coupled coding — SG2–SG5 are serial, single-repo.
- Context hygiene — targeted grep over full re-Read.
- Shift boundaries — checkpoint via handoff → clear → continue; caps cumulative.
- Scope: `/home/wtthornton/code/HomeIQ` only. Reads may go wider; writes do not.
- Memory: recall at start; record a structured handoff at each checkpoint.
- **Research grant:** this loop has web access, `tapps_research`, and `tapps_lookup_docs`
  (Context7-backed, local-cache-first — effectively free to repeat). **Required lookups
  before the first line touching an external surface:** the HA release list (to resolve
  the pin), HA custom-component `/config/custom_components/` layout, and the Powercalc and
  Team Tracker release tags being vendored. Do not write against these from memory.
- **Harness compatibility:**
  - `tapps_quick_check(file_path)` after each Python edit (`scripts/check-ha-component-manifest.py`);
    `tapps_validate_changed(file_paths=...)` before declaring done. **Adopted**, not overridden.
  - `tapps_validate_config` on `compose.yml` and the new Dockerfile. Note TAP-6494: it
    rejects the absolute path its own remediation demands — pass a repo-relative path.
  - Linear writes are hook-gated by a validation sentinel (<30 min). Route through the
    `linear-issue` skill; re-satisfy the gate if the loop has outlived the sentinel.
  - This prompt **is** the user's instruction to dispatch the verifier subagent named in
    the Plane map; the session's "no subagents unless requested" default does not block it.
  - CI reds carry no signal here: `E2E & Integration Tests` and `CI — ML Engine` are red on
    master head; `Docker Build and Test` / `Docker Test` are 12/12 red on every branch.
    Always diff a PR's reds against master head before treating one as caused by this work.
- Discipline: root-cause not workarounds; no green-by-suppression; right-sized; durable;
  match repo conventions; no scope creep.

## Autonomy

- Act on every reversible, in-scope step — no "should I proceed?" checkpoints.
- Irreversible/outward step → produce the reversible precursor (draft PR, staged diff) and
  continue; the human reviews async. A draft PR is not a stop.
- Hard-stop once (batched, with a recommendation) only for: **merging to master** ·
  **enabling HACS at runtime to make a component appear** · **any write outside this repo**
  · **a raw Linear `save_issue` bypassing the skill** · irreversible/outward with no
  precursor · projected next-step cost > USD 20 · the validation contract itself being wrong.

## Failure handling

- On failed verify: diagnose (error + state + brain recall) → hypothesis → fix → retry
  *differently*. ≤3 distinct strategies per sub-goal, then escalate once, then stop with a
  concise diagnosis. Never repeat the same action on the same error.
- Expected-fail: the first verify failure is normal — scope a fix sub-goal, do not panic
  and do not rewrite the contract.

## Unverified assumptions

TAP-6483's body carries three claims that did not survive a check on 2026-08-23. Confirm
each in SG1 before depending on it.

- **`hacs.json` still exists** (ticket `## Where` item 5, AC7) — **FALSE.** Already deleted
  by commit `32166a1c`. AC7 is met; verify and record, do not redo.
  Confirm by: `git log --oneline -1 -- hacs.json`.
- **"A seeded `configuration.yaml`"** (AC6) — **does not exist.** `compose.yml:693-695`
  mounts the *named volume* `ha_test_config`, not a bind mount, so HA generates a default
  config on first boot and nothing is checked in. SG5 must create the seed; AC6 is not
  satisfiable as written until it does.
  Confirm by: `find . -name configuration.yaml` returns nothing.
- **"`stable` is the only unpinned tag in the repo"** (ticket `## Why`) — **FALSE.**
  `domains/data-collectors/compose.yml:345` is `image: homeiq/zeek:latest`. Out of scope
  here; note it rather than fixing it, and do not let the false claim justify a wider diff.
  Confirm by: `grep -rn 'image:.*:stable\|image:.*:latest' --include='compose*.yml' .`
- **The FP1E quirk is not a custom_component** (ticket AC2 lists all four under
  `/config/custom_components/`) — **FALSE.** A ZHA quirk loads only from the
  directory named by `zha.custom_quirks_path`; it belongs in
  `/config/custom_zha_quirks/`. Placing it in `custom_components/` would load
  nothing. Confirm by: `libs/homeiq-ha/src/homeiq_ha/agent/zha_quirks.py:64`.
- **Line anchors drifted.** The fixture is at `compose.yml:687-712`, not the `668-688` the
  ticket cites. Re-anchor before editing.

## Context

- Repo: `/home/wtthornton/code/HomeIQ` · Linear team `TappsCodingAgents` · project `HomeIQ`
- Story: TAP-6483 (Urgent, In Progress) · parent epic TAP-6464 · blocks TAP-6484/6485/6486
- Decisions: `docs/architecture/adr-appliance-packaging.md`
- Anchors: `domains/core-platform/compose.yml:687-712` (fixture),
  `custom_components/homeiq/manifest.json:13` (`min_ha_version` floor `2026.8.0`),
  `libs/homeiq-ha/src/homeiq_ha/agent/quirks/aqara_fp1e.py`,
  `libs/homeiq-ha/src/homeiq_ha/agent/powercalc.py:63-95`,
  `.github/workflows/quality-gate.yml:64` (CI-check precedent),
  `scripts/check-container-budget.py` (script precedent)

## Run-as

Cold-start loop — paste the "How to run" line into a fresh session. Not a Workflow (N=1).
