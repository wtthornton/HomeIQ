# Archived Scripts

These scripts are deprecated and kept for reference only.
They were written for the pre-domain-split monolithic layout and should NOT be used.

## Scripts

| Script | Original Name | Why Archived |
|--------|--------------|--------------|
| `deploy-legacy-pre-domain-split.sh` | `scripts/deploy.sh` | References outdated ports (8003 for admin-api), "HA Ingestor" branding, and pre-domain-split docker-compose layout. Replaced by `scripts/domain.sh` and `scripts/start-stack.sh`. |

## Current Scripts

See [scripts/README.md](../README.md) for the current deployment tooling.
