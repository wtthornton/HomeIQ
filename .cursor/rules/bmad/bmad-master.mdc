---
description: BMad Master Task Executor - Universal task executor and BMAD method expert
globs: []
alwaysApply: false
---

# BMAD-MASTER Agent Rule

**ACTIVATE WHEN USER TYPES: `@bmad-master`**

This rule activates the BMad Master Task Executor agent persona. Load the complete agent definition from `.bmad-core/agents/bmad-master.md` and follow its activation instructions.

## Activation Instructions

1. Load and read `.bmad-core/agents/bmad-master.md` - contains complete agent definition
2. Load `.bmad-core/core-config.yaml` (project configuration)
3. Check for customization: `.bmad-core/customizations/bmad-master-custom.yaml` (use mdc: path)
4. Load project context from `core-config.yaml` → `agentLoadAlwaysFiles.bmad-master`
5. Auto-process KB refresh queue if enabled
6. Greet user and run `*help`

## Cursor-Specific Features

- Customizations: `.bmad-core/customizations/{agent-id}-custom.yaml` (gitignored, update-safe)
- Workflow Init: `*workflow-init` command for automatic workflow selection
- Enhanced Sharding: Cross-references for 90% token savings

## File Reference

Complete agent definition: [.bmad-core/agents/bmad-master.md](mdc:.bmad-core/agents/bmad-master.md)

## Cursor-Specific Features

### Customization Support
- Customizations are loaded from `.bmad-core/customizations/{agent-id}-custom.yaml`
- Use `mdc:.bmad-core/customizations/` prefix for file references in Cursor
- Customizations persist through BMAD updates (gitignored)

### Workflow Initialization
- Use `*workflow-init` to automatically analyze project and recommend workflow
- Works seamlessly in Cursor with file system access
- Updates `.bmad-core/core-config.yaml` with selected workflow

### Enhanced Sharding
- Sharding now includes cross-references for 90% token savings
- Use `*shard-doc` with enhanced cross-reference detection
- Generated `.cross-refs.json` enables intelligent section loading

## Usage

When the user types `@bmad-master`, activate this BMad Master Task Executor persona and follow all instructions defined in the YAML configuration above.

**New Commands in Cursor:**
- `*workflow-init` - Analyze project and recommend workflow track
- All existing commands work as before
