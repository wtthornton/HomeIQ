{
  "description": "Enhance HA AI Agent ContextBuilder service to include device state context (current entity states when mentioned in user prompt), recent automation patterns (last 5-10 automations created by user), conflict detection (check for overlapping triggers/actions with existing automations), and user enhancement preferences (track and include previously selected enhancement types). Implementation should integrate new context services into ContextBuilder, maintain backward compatibility with existing context caching and token budget limits, use async/await patterns, implement graceful degradation, and add appropriate caching strategies. Focus on Phase 1 (Device State Context) as the first deliverable.",
  "instruction": {
    "agent_name": "analyst",
    "command": "analyze-requirements",
    "prompt": "Analyze the following requirement description and extract detailed requirements.\n\nDescription:\nEnhance HA AI Agent ContextBuilder service to include device state context (current entity states when mentioned in user prompt), recent automation patterns (last 5-10 automations created by user), conflict detection (check for overlapping triggers/actions with existing automations), and user enhancement preferences (track and include previously selected enhancement types). Implementation should integrate new context services into ContextBuilder, maintain backward compatibility with existing context caching and token budget limits, use async/await patterns, implement graceful degradation, and add appropriate caching strategies. Focus on Phase 1 (Device State Context) as the first deliverable.\n\n\n\nPlease provide:\n1. Functional Requirements (what the system should do)\n2. Non-Functional Requirements (performance, security, scalability, etc.)\n3. Technical Constraints\n4. Assumptions\n5. Open Questions\n\nFormat as structured JSON with sections.",
    "parameters": {
      "description": "Enhance HA AI Agent ContextBuilder service to include device state context (current entity states when mentioned in user prompt), recent automation patterns (last 5-10 automations created by user), conflict detection (check for overlapping triggers/actions with existing automations), and user enhancement preferences (track and include previously selected enhancement types). Implementation should integrate new context services into ContextBuilder, maintain backward compatibility with existing context caching and token budget limits, use async/await patterns, implement graceful degradation, and add appropriate caching strategies. Focus on Phase 1 (Device State Context) as the first deliverable."
    }
  },
  "skill_command": "@analyst analyze-requirements --description \"Enhance HA AI Agent ContextBuilder service to include device state context (current entity states when mentioned in user prompt), recent automation patterns (last 5-10 automations created by user), conflict detection (check for overlapping triggers/actions with existing automations), and user enhancement preferences (track and include previously selected enhancement types). Implementation should integrate new context services into ContextBuilder, maintain backward compatibility with existing context caching and token budget limits, use async/await patterns, implement graceful degradation, and add appropriate caching strategies. Focus on Phase 1 (Device State Context) as the first deliverable.\""
}