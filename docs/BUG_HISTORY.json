[
    {
        "date":  "2026-03-10 08:26",
        "run_id":  "master",
        "branch":  "master",
        "bugs":  [
                     {
                         "pr_merged":  null,
                         "description":  "create_shared_session_maker() checks `if database_url not in _session_makers` without holding `_engine_lock`. Two concurrent callers can both find the key missing, both create a session maker, and the second overwrites the first in the dict. Any sessions already created from the first session maker are now orphaned ÃƒÅ½Ã¢â‚¬Å“ÃƒÆ’Ã¢â‚¬Â¡ÃƒÆ’Ã‚Â¶ their parent session maker is no longer tracked, so those connections leak out of the pool accounting. The engine creation directly below IS protected by _engine_lock, but session maker creation is not.",
                         "line":  102,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "libs/homeiq-data/src/homeiq_data/database_pool.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "allow_request() reads _half_open_calls and _state without holding _lock, and the caller must separately call increment_half_open_calls() in a distinct async step. This TOCTOU race means N concurrent coroutines can all call allow_request() before any of them calls increment_half_open_calls(), and all see _half_open_calls == 0 \u003c half_open_max_calls (default 1), causing all N to return True. This floods the recovering upstream with concurrent probe requests instead of the intended single trial, defeating HALF_OPEN limiting entirely.",
                         "line":  129,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "libs/homeiq-resilience/src/homeiq_resilience/circuit_breaker.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "get_all_context() breaks out of the loop entirely on the first context whose size would exceed the budget (when contexts is non-empty). Contexts are sorted by relevance score descending, so a large but highly-relevant context will cause all subsequent smaller contexts ÃƒÅ½Ã¢â‚¬Å“ÃƒÆ’Ã¢â‚¬Â¡ÃƒÆ’Ã‚Â¶ which could fit within the remaining budget ÃƒÅ½Ã¢â‚¬Å“ÃƒÆ’Ã¢â‚¬Â¡ÃƒÆ’Ã‚Â¶ to be silently skipped. The LLM receives incomplete context, causing incorrect automation suggestions and missed domain knowledge for all 50+ downstream services using this registry.",
                         "line":  129,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "libs/homeiq-patterns/src/homeiq_patterns/rag_context_registry.py"
                     }
                 ],
        "chain":  false
    },
    {
        "date":  "2026-03-10 13:04",
        "run_id":  "master",
        "branch":  "master",
        "bugs":  [
                     {
                         "pr_merged":  null,
                         "description":  "Flux query injection: the `service_name` parameter is directly interpolated into the Flux filter string (`r.service == \"{service_name}\"`) with no sanitization. An attacker who controls `service_name` can inject arbitrary Flux clauses (e.g., closing the string and appending `|\u003e drop()` or piping to a second query), altering query semantics or exfiltrating data from the bucket.",
                         "line":  189,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "domains/core-platform/admin-api/src/influxdb_client.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "Type mismatch between the service return type and the declared response_model. `service.create_backup()` returns a `BackupInfo` dataclass (backup_restore.py) that has `success: bool` and no `status` field, but the endpoint is declared `response_model=BackupResponse` which requires `status: str` (no default). FastAPI\u0027s response validation raises a ResponseValidationError (HTTP 500) on every backup creation request.",
                         "line":  39,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "domains/core-platform/data-retention/src/api/routers/backup.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "Missing `self.enabled` guard in `log_retention_operation()`. Every other public method in this class checks `if not self.enabled: return` before using `self.client`, but `log_retention_operation` does not. When storage analytics is disabled (the default case with InfluxDB 2.7, where `self.client` is None), calling `self.client.write(point)` raises AttributeError. The surrounding try/except silently swallows the error, causing all retention operation audit records to be silently dropped.",
                         "line":  173,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "domains/core-platform/data-retention/src/storage_analytics.py"
                     }
                 ],
        "chain":  false
    },
    {
        "date":  "2026-03-10 14:52",
        "run_id":  "master",
        "branch":  "master",
        "bugs":  [
                     {
                         "pr_merged":  null,
                         "description":  "String comparison used for model version check: `model \u003e= \"gpt-5.2\"` is lexicographic. A future model like \"gpt-5.10\" compares as less than \"gpt-5.2\" (because \u00271\u0027 \u003c \u00272\u0027 in ASCII), misclassifying it as a non-reasoning model. This incorrectly adds a `temperature` parameter to the request instead of `reasoning`, causing an API error from OpenAI.",
                         "line":  174,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "domains/automation-core/ha-ai-agent-service/src/services/openai_client.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "`area_keywords` is a Python `set` literal. Set iteration order is non-deterministic across processes due to Python hash randomization (PYTHONHASHSEED). When a user prompt contains multiple area names (e.g. \"turn on bedroom and kitchen lights\"), the first matched area varies per-process, making entity resolution results non-deterministic for the same input.",
                         "line":  174,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "domains/automation-core/ha-ai-agent-service/src/services/entity_resolution/entity_resolution_service.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "`create_conversation` is declared as `-\u003e Conversation` but falls through to `return None` at line 138 when `get_session()` yields no sessions (e.g. during a database outage). Any caller that accesses the return value (e.g. `conv.conversation_id`) will crash with `AttributeError: \u0027NoneType\u0027 object has no attribute \u0027conversation_id\u0027`.",
                         "line":  138,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "domains/automation-core/ha-ai-agent-service/src/services/conversation_service.py"
                     }
                 ],
        "chain":  false
    },
    {
        "date":  "2026-03-10 15:00",
        "run_id":  "master",
        "branch":  "master",
        "bugs":  [
                     {
                         "pr_merged":  null,
                         "description":  "When all motion sensors are inactive (state in {\u0027unavailable\u0027,\u0027unknown\u0027}), `active_sensors` ends up empty. The guard `if active_sensors: motion_sensors = active_sensors` silently skips the update, so the original unfiltered list of unavailable sensors is written to `resolved[\u0027motion_sensor_entities\u0027]`. The automation is then generated referencing sensors that HA cannot use.",
                         "line":  347,
                         "was_real":  null,
                         "severity":  "high",
                         "file":  "domains/automation-core/ai-automation-service-new/src/services/template_validator.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "In `validate_entities`, any exception (e.g. transient data-API failure) returns `(False, [])`. Every caller treats this as \u0027invalid entities found\u0027 and raises `YAMLGenerationError(f\u0027...Invalid entities found: \u0027)` with an empty entity list, producing a misleading error message and discarding a potentially valid automation. YAML generation is broken for the duration of any data-API outage.",
                         "line":  643,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "domains/automation-core/ai-automation-service-new/src/services/yaml_generation_service.py"
                     },
                     {
                         "pr_merged":  null,
                         "description":  "In `rebuild_from_yaml`, `triggers = yaml_data.get(\u0027trigger\u0027) or yaml_data.get(\u0027triggers\u0027, [])` does not handle the common case where `trigger` is a single dict (not a list). Iterating `for trigger in triggers` then iterates over the dict\u0027s **keys** (strings), so `trigger.get(\u0027platform\u0027)` raises `AttributeError: \u0027str\u0027 object has no attribute \u0027get\u0027`. The exception is silently caught and the method returns `None` for every single-trigger HA automation.",
                         "line":  199,
                         "was_real":  null,
                         "severity":  "medium",
                         "file":  "domains/automation-core/ai-automation-service-new/src/services/json_verification_service.py"
                     }
                 ],
        "chain":  false
    }
]