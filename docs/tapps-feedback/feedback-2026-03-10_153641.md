# TappsMCP Feedback - 2026-03-10 15:36

**Run context**: master branch, automated bugfix session, 5 bugs fixed across 5 Python files in 4 automation-core services.

**Files changed**: `sandbox.py`, `processor.py`, `generator.py`, `dedup_tracker.py`, `normalizer.py`

---

### [UX] P1: Server root resolves to /workspace instead of project root
- **Date**: 2026-03-10 15:36
- **Run**: master
- **Tool**: tapps_session_start
- **Detail**: `tapps_session_start()` reports `project_root: /workspace` (Docker container path). The actual project root is `C:\cursor\HomeIQ`. This means all file-path arguments passed to subsequent tools must use container-relative paths, which creates friction in automated runs where paths come from `git show` output. Expected: path resolution to match the host working directory or surface the mismatch clearly. Actual: silent mismatch — the server accepts calls but file-scoped tools silently use `/workspace` context.
- **Recurrence**: 3 (observed in prior sessions; see sprints.md)

---

### [ENHANCEMENT] P2: tapps_validate_changed could report per-file gate result in summary line
- **Date**: 2026-03-10 15:36
- **Run**: master
- **Tool**: tapps_validate_changed
- **Detail**: When run against 5 changed Python files across 4 different services, the batch output is correct and comprehensive. However, the summary does not emit a single-line per-file pass/fail that can be grepped in CI logs (e.g., `PASS sandbox.py | FAIL processor.py`). Expected: structured per-file result rows in output. Actual: consolidated narrative report. This is minor but useful for automated log parsing.
- **Recurrence**: 1

---

### [FALSE_NEGATIVE] P1: tapps_quick_check did not surface the processor.py kwarg mismatch before fix
- **Date**: 2026-03-10 15:36
- **Run**: master
- **Tool**: tapps_quick_check
- **Detail**: `processor.py` called `suggestion_generator.generate()` with kwargs `user_id`, `clarification_context`, `query_id`, `db` — but the actual `generate()` signature uses `_user_id`, `_clarification_context`, `_query_id`, `_db` (underscore-prefixed to mark them as unused/placeholder). Mypy would catch this as `Unexpected keyword argument` only if the callee's type stubs are resolved; ruff would not catch it. Pre-fix, `tapps_quick_check` should have flagged this via mypy. If it did not, it is a false negative at the mypy integration layer. Post-fix the check passes cleanly. Root cause is likely that mypy's cross-file analysis requires the full package to be importable, which may not hold in the container environment.
- **Recurrence**: 1

---

### [INTEGRATION] P2: tapps_checklist final step completes correctly but lacks run-id linkage
- **Date**: 2026-03-10 15:36
- **Run**: master
- **Tool**: tapps_checklist
- **Detail**: The checklist ran as the final verify stage and confirmed process was followed. Works as designed. Enhancement opportunity: checklist output does not include the git commit SHA or branch so the audit trail in `TAPPS_RUNLOG.md` must be manually correlated. Expected: checklist response includes `{"branch": "master", "head_sha": "5034978e"}` in its metadata. Actual: no commit linkage in output.
- **Recurrence**: 1

---

### [PERF] P2: tapps_session_start takes ~4s on cold start in automated runs
- **Date**: 2026-03-10 15:36
- **Run**: master
- **Tool**: tapps_session_start
- **Detail**: Cold-start duration was 3,953 ms (`server_info_ms: 3217`, `memory_status_ms: 657`). In an automated bugfix pipeline that may run multiple sessions per day, this adds meaningful latency. The `quick=True` mode exists but skips diagnostics needed for accurate tool routing. Expected: warm cache reduces subsequent calls. Actual: cache directory `/workspace/.tapps-mcp-cache` did not exist (`exists: false`, `writable: false`), so no caching benefit was available.
- **Recurrence**: 2

---

## Overall Assessment

All three core tools (`tapps_validate_changed`, `tapps_checklist`, `tapps_quick_check`) performed their intended roles in this session. The 5 bugs fixed cover meaningful failure modes (deprecated API, type annotation race condition, kwarg mismatch, missing parameter, insertion-order data structure). No tool produced a blocking false positive that would have stopped a valid fix. The primary recurring issue remains the `/workspace` path mismatch and cold-start cache miss.
