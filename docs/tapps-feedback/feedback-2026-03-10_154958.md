# TappsMCP Feedback - 2026-03-10 15:49

**Run context**: master branch, automated bugfix session, 3 bugs fixed across 2 Python files in 2 ml-engine services.

**Files changed**: `service_manager.py` (ai-core-service), `fine_tune_peft.py` (nlp-fine-tuning), plus doc/story/script updates.

---

### [PERF] P2: tapps_session_start cold-start improved significantly vs prior run
- **Date**: 2026-03-10 15:49
- **Run**: master
- **Tool**: tapps_session_start
- **Detail**: This session's `tapps_session_start` completed in 1,019 ms (`server_info_ms: 902`, `memory_status_ms: 97`), down from 3,953 ms in the 15:36 run. Cache directory `/workspace/.tapps-mcp-cache` still does not exist (`exists: false`, `writable: false`), so the speedup is likely due to reduced Docker layer I/O or server warm state rather than caching. The `/workspace` vs `C:\cursor\HomeIQ` path mismatch persists — all file-path args to subsequent tools must use container-relative paths. Expected: cache to be writable so warm calls stay < 500ms consistently. Actual: cache directory absent each run; latency varies between ~1s and ~4s unpredictably.
- **Recurrence**: 3 (perf issue); 4 (path mismatch UX issue — see feedback-2026-03-10_153641.md)

---

### [FALSE_NEGATIVE] P1: tapps_quick_check may not catch cross-file type errors in ml-engine services
- **Date**: 2026-03-10 15:49
- **Run**: master
- **Tool**: tapps_quick_check
- **Detail**: 3 bugs were fixed in this session across `service_manager.py` and `fine_tune_peft.py`. The bugs required manual identification via the bug-scanner agent before `tapps_quick_check` was applied post-fix. This follows the same pattern noted in the 15:36 run: `tapps_quick_check` validates quality *after* fixing but does not proactively surface bugs that require cross-file or cross-package type resolution (e.g., incorrect method signatures, missing async context manager handling). Mypy's cross-file analysis in the container environment is limited because the full package graph is not importable. Expected: pre-fix `tapps_quick_check` should surface the same issues as the bug-scanner. Actual: bugs are found by a separate scanner step; `tapps_quick_check` confirms the fix is clean.
- **Recurrence**: 2 (same pattern as 15:36 run)

---

### [ENHANCEMENT] P2: tapps_validate_changed per-file pass/fail rows still absent
- **Date**: 2026-03-10 15:49
- **Run**: master
- **Tool**: tapps_validate_changed
- **Detail**: Batch validation ran correctly against the 2 changed Python files and passed. The consolidated narrative output is accurate but still lacks machine-readable per-file rows (e.g., `PASS service_manager.py | PASS fine_tune_peft.py`) that automated log parsers or CI step summaries could consume without regex parsing. No regression from prior run — enhancement request stands.
- **Recurrence**: 2 (see feedback-2026-03-10_153641.md)

---

### [INTEGRATION] P2: tapps_checklist still lacks git commit SHA linkage
- **Date**: 2026-03-10 15:49
- **Run**: master
- **Tool**: tapps_checklist
- **Detail**: Final verify step completed successfully. Checklist confirmed all pipeline stages were followed for the bugfix task type. No functional issues. The enhancement request from the 15:36 session stands: checklist output does not include branch or HEAD SHA, requiring manual correlation in `TAPPS_RUNLOG.md`. Actual commit: `a80f38c7`.
- **Recurrence**: 2 (see feedback-2026-03-10_153641.md)

---

## Overall Assessment

All three core tools (`tapps_quick_check`, `tapps_validate_changed`, `tapps_checklist`) performed correctly for their scoped responsibilities in this session. No blocking false positives. The primary structural limitation remains that `tapps_quick_check` operates as a post-fix quality gate rather than a pre-fix bug detector — the bug-scanner agent fills that gap. The `/workspace` path mismatch is the most impactful recurring friction point (now at recurrence 4) and warrants an upstream fix or a session-start warning that surfaces the expected path mapping explicitly.
