# TappsMCP Feedback - 2026-03-10 11:12

**Run context**: Automated bugfix run (`scripts/auto-bugfix-parallel.ps1`) on branch `master`.
3 bugs fixed across `core-platform` domain (api_key_service.py, tiered_retention.py, event_queue.py).
MCP config files also updated (.cursor/mcp.json, .mcp.json). Second run of the day (first: 09:36).

---

### [PERF] P2: tapps_quick_check no batch mode — recurrence confirmed in parallel script
- **Date**: 2026-03-10 11:12
- **Run**: master (automated bugfix — parallel PowerShell)
- **Tool**: tapps_quick_check
- **Detail**: `tapps_quick_check` still requires one call per file. The new parallel script (`auto-bugfix-parallel.ps1`) mitigates this by launching concurrent PowerShell jobs per file, but each job still spins up a full MCP call. At 3 files overhead is acceptable; at scale the per-call startup cost dominates. A native batch mode accepting `[file1, file2, ...]` would eliminate redundant process startups. Expected: `tapps_quick_check(files=["a.py","b.py"])`. Actual: loop required.
- **Recurrence**: 2

---

### [UX] P2: tapps_checklist verbose output in CI/automated logs — recurrence confirmed
- **Date**: 2026-03-10 11:12
- **Run**: master (automated bugfix — parallel PowerShell)
- **Tool**: tapps_checklist
- **Detail**: `tapps_checklist(task_type="bugfix")` completed successfully. Full markdown table output again captured verbatim in script logs. No regression — same UX gap as 09:36 run. A `compact=True` or `format="json"` flag would reduce automated log noise. Expected: machine-readable summary. Actual: ~30-line markdown table.
- **Recurrence**: 2

---

### [INTEGRATION] P1: tapps_validate_changed base_ref edge case — recurrence confirmed
- **Date**: 2026-03-10 11:12
- **Run**: master (automated bugfix — parallel PowerShell)
- **Tool**: tapps_validate_changed
- **Detail**: Parallel script commits each fix before calling `tapps_validate_changed`, so the base_ref=HEAD issue (silent empty diff on staged-only changes) was again avoided by script discipline rather than tool guardrail. Issue structurally unchanged from 09:36 run. A warning when `base_ref=HEAD` detects zero diff would catch misconfigured callers. Actual: silent pass.
- **Recurrence**: 2

---

### [ENHANCEMENT] P2: tapps_validate_config not called for MCP config changes
- **Date**: 2026-03-10 11:12
- **Run**: master (automated bugfix — parallel PowerShell)
- **Tool**: tapps_validate_changed
- **Detail**: This run changed `.cursor/mcp.json` and `.mcp.json` (MCP server configuration files). `tapps_validate_changed` only covers Python files — non-Python config changes are outside its scope. No tool in the pipeline validated the JSON config edits. `tapps_validate_config` targets Dockerfile/docker-compose; it does not cover MCP JSON configs. Expected: either `tapps_validate_changed` flags non-Python infra files, or `tapps_validate_config` is extended to cover MCP server configs. Actual: config changes shipped unvalidated by TappsMCP.
- **Recurrence**: 1

---

## Summary

All three tools functioned correctly — no crashes or incorrect gate decisions. Two P2 issues
(PERF batch mode, UX verbose output) and one P1 (INTEGRATION base_ref edge case) are confirmed
recurring from the 09:36 run. One new ENHANCEMENT identified: MCP config file changes fall
outside current tool coverage.

| Tool | Result | Issues |
|---|---|---|
| `tapps_validate_changed` | PASS | INTEGRATION P1 (base_ref, recur=2), ENHANCEMENT P2 (config coverage, recur=1) |
| `tapps_checklist` | PASS | UX P2 (verbose CI output, recur=2) |
| `tapps_quick_check` | PASS | PERF P2 (no batch mode, recur=2) |
