# TappsMCP Feedback - 2026-03-10 09:36

**Run context**: Automated bugfix run (`scripts/auto-bugfix.sh`) on branch `master`.
3 bugs fixed across `core-platform` domain. No previous feedback files found (first entry).

---

### [ENHANCEMENT] P2: tapps_validate_changed could surface per-bug diff context in automated runs
- **Date**: 2026-03-10 09:36
- **Run**: master (automated bugfix)
- **Tool**: tapps_validate_changed
- **Detail**: `tapps_validate_changed` ran against the 3 changed Python files (`api_key_service.py`, `scheduler.py`, `connection_manager.py`) and passed the quality gate (standard preset). All files were ruff/bandit clean post-fix. However, in automated runs the tool output is consumed by the script without structured per-bug traceability — there is no way to correlate which quality finding triggered which fix from the tool output alone. Expected: a structured `bug_id` or `fix_ref` in the output. Actual: flat list of files + pass/fail per file.
- **Recurrence**: 1

---

### [UX] P2: tapps_checklist final step produces verbose output that inflates automated run logs
- **Date**: 2026-03-10 09:36
- **Run**: master (automated bugfix)
- **Tool**: tapps_checklist
- **Detail**: `tapps_checklist(task_type="bugfix")` completed successfully and all gates passed. In an automated script context (no interactive terminal), the checklist output is written to stdout and captured verbatim in the commit message pipeline. The rendered markdown table (~30 lines) is unnecessarily large for CI/automated logs. Expected: a `compact=True` flag or machine-readable JSON summary mode. Actual: full markdown table always rendered.
- **Recurrence**: 1

---

### [PERF] P2: tapps_quick_check took ~3-5s per file in quick=True mode — acceptable but notable at scale
- **Date**: 2026-03-10 09:36
- **Run**: master (automated bugfix)
- **Tool**: tapps_quick_check
- **Detail**: `tapps_quick_check` was called on each of the 3 Python files post-edit. Ruff-only mode (`quick=True`) completed without errors and correctly caught no remaining issues after fixes were applied. Timing observed ~3-5s per file on Windows (WSL/bash via PowerShell wrapper). At 3 files this is fine; at 20+ files in a larger bugfix run the serial calls would add 60-100s to the automated pipeline. Expected: a batch mode accepting multiple files. Actual: one file per call, must loop.
- **Recurrence**: 1

---

### [INTEGRATION] P1: tapps_validate_changed base_ref must be set explicitly in automated scripts — default HEAD misses staged-only changes
- **Date**: 2026-03-10 09:36
- **Run**: master (automated bugfix)
- **Tool**: tapps_validate_changed
- **Detail**: When `auto-bugfix.sh` calls `tapps_validate_changed` with default `base_ref=HEAD`, it diffs against HEAD (unstaged). If fixes are staged but not committed yet (common in pre-commit hook scenarios), the tool sees no diff and skips validation entirely — a silent false-pass. The run avoided this because fixes were committed before validate, but the script has no guard. Expected: documentation or warning when `base_ref=HEAD` and no unstaged changes are detected. Actual: silent empty result treated as "nothing to validate → pass".
- **Recurrence**: 1

---

## Summary

All three tools (`tapps_validate_changed`, `tapps_checklist`, `tapps_quick_check`) functioned correctly for this run — no bugs, crashes, or incorrect quality gate decisions. Issues noted are enhancement/UX opportunities for automated pipeline contexts, not failures. No tool regressions detected versus previous sessions.

| Tool | Result | Issues |
|---|---|---|
| `tapps_validate_changed` | PASS | ENHANCEMENT (per-bug traceability), INTEGRATION (base_ref edge case) |
| `tapps_checklist` | PASS | UX (verbose output in CI) |
| `tapps_quick_check` | PASS | PERF (no batch mode) |
